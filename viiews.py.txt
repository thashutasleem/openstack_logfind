from django.shortcuts import render
from django.http import HttpResponse
from . models import horizon_log


def home(request):
    return render(request,'home.html')

def horizon(request):
    return render(request,'horizon.html')

def image(request):
    return render(request,'image.html')

def instance(request):
    return render(request,'instance.html')

def network(request):
    return render(request,'network.html')

def volume(request):
    return render(request,'volume.html')

def logout(request):
    return render(request,'logout.html')

def horizon_date(request):
    # data=access.objects.order_by('id')[:5]
    # data_dict={ "data" :data     }
    count=horizon_log.objects.all().count()
    count_dict={"count":count}
    # data_dict=get_data_dict(data)
    return render(request,'horizon_date.html',count_dict)
